<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content="Roadworks information." />
    <link rel="icon" href="favicon.png" type="image/png"/>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <title>Roadworks</title>
    <%- head_end %>
    <style>
      .arrows {
        max-width: 1rem;
      }
      #contentTypeSearchBtn,
      #contentTypeSearchInput {
        max-width: 40rem;
        border-radius: 0px;
        border: 2px solid #4b5458;
      }
      .page-item.disabled .page-link {
        color: #555c62;
        border: 2px solid #4b5458;
      }
      .page-link {
        color: #0248b1;
        border: 2px solid #4b5458;
        margin: 0 22px 22px 0;
      }
      .page-item button {
        min-width: 6rem;
      }
      .page-link {
        border-radius: 7px !important;
      }
      table {
        width: 100%;
      }
      button {
        color: Black;
      }
      .black {
        color: Black !important;
      }
      #upstartDate {
        color: Black;
      }
      #description {
        width: 50% !important;
      }
      .tableHead {
        cursor: pointer;
        min-width: 8rem;
        height: 3rem;
      }
      .tableHead:hover {
        background: PaleGreen;
      }
      .entryUpIcon,
      .entryDownIcon {
        color: gray;
        position: relative;
      }
      .entryUpIcon {
        top: -0.5rem;
      }
      .entryDownIcon {
        bottom: -0.4rem;
        right: 0.77rem;
      }
      table,
      th,
      td {
        border: 1px solid;
        background-color: white;
      }
      td {
        vertical-align: top;
        padding-top: 0.5rem;
      }
      table {
        border-collapse: collapse;
      }
      th,
      td {
        padding-left: 0.3rem;
        padding-right: 0.3rem;
      }
      .fakeLink {
        cursor: pointer;
        color: blue;
        text-decoration: underline;
      }
      .fakeLink:hover {
        color: darkblue;
      }
      @supports (-webkit-appearance: none) {
        .entryUpIcon {
          top: -0.4rem;
        }
        .entryDownIcon {
          bottom: -0.3rem;
          right: 1rem;
        }
      }
      @supports (-moz-appearance: none) {
        .entryUpIcon {
          top: -0.6rem;
        }
        .entryDownIcon {
          bottom: -0.3rem;
          right: 0.77rem;
        }
      }
    </style>
  </head>
  <body>
    <div class="container mt-3">
      <div class="row">
        <h1>Roadworks</h1>
    <p class="mb-0"><strong>SSR version</strong></p>
        <div id="app" class="mt-2"><%- html %></div>
      </div>
    </div>
  </body>
</html>
